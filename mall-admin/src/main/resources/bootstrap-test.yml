spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        shared-configs:
          - data-id: sentinel-config.yaml  # 指定 Nacos 配置中的 Data ID
            group: DEFAULT_GROUP            # 指定 Group ID
#    sentinel:
#      transport:
#        dashboard: localhost:18080
#        port: 18719
#      eager: true
